# Tasks: Shell Theme Density Baseline

## 1. Define the shell presentation contract
- [ ] Add a requirement for compact, tool-dense shell presentation on docked and editor surfaces.
- [ ] Confirm the requirement stays at contract level rather than exact token values.

## 2. Define theme ownership boundaries
- [ ] Add a requirement for base-shell theme responsibilities.
- [ ] Add a requirement that system overlays attach to shared theme slots without changing shell roles.

## 3. Define scene art-direction baseline
- [ ] Add a requirement for the default stylized low-fidelity 3D direction.
- [ ] Confirm the baseline prioritizes tabletop readability over realism-heavy rendering.

## 4. Validate the change
- [ ] Run `npx openspec validate shell-theme-density-baseline`.
- [ ] Confirm the change avoids exact visual token commitments and implementation-library details.